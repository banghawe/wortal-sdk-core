/**
 * Options for sorting tournament entries.
 * @hidden
 */
export type SortOrder = "HIGHER_IS_BETTER" | "LOWER_IS_BETTER";

/**
 * Options for tournament score formats.
 * @hidden
 */
export type ScoreFormat = "NUMERIC" | "TIME";
