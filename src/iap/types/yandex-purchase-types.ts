/**
 * Currency code for Yandex. Yandex uses their own platform currency so all IAP transactions are in YAN.
 * @hidden
 */
export type CurrencyCode_Yandex = "YAN";

/**
 * Sizes for the YAN currency image.
 * @hidden
 */
export type CurrencyImageSize = "small" | "medium" | "svg";
