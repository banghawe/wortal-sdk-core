/**
 * The type of stat this value represents.
 */
export type StatValueType = "number" | "time";

/**
 * The period of time over which the stat is tracked.
 */
export type StatPeriod = "alltime" | "daily" | "monthly" | "weekly";
